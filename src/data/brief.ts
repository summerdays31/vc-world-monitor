import type { BriefSection } from "./types";

/** @deprecated Brief page is built from the live bundle — kept empty. */
export const briefMeta = {
  title: "Sourced instruments",
  subtitle: "Built from the live / curated bundle",
  asOf: "",
  disclaimer: "No EXAMPLE editorial on this route.",
};

export const briefSections: BriefSection[] = [];
